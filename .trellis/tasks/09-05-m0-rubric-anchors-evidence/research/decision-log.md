# 已确认决定与被否决方案

初始记录日期：2026-09-05；最近需求更新：2026-09-06。来源：本次用户对话。后续最新答复优先于本文件与历史规划。

## 已确认

| 编号 | 决定 |
| --- | --- |
| D01 | M0 只收题、保存材料并生成和审改维度；新稿与实际评分属于 M1。 |
| D02 | 新颖性可以成为维度，通过分可以很低；每项固定 10 分、逐项通过，不要求每项都优秀。 |
| D03 | 标准答案是老师已认可的正向依据，不另起一套外来标准否定老师；M0 不因此增加标准答案评分执行。 |
| D04 | 增加分档说明，帮助老师理解和设置通过分；这是 M0 的规则制定辅助。 |
| D05 | 保留现有勾选维度、编辑内容、填写整数通过分的交互；AI 建议值和人工设定值都可为 0–10 任意整数。 |
| D06 | 分档用明确分数的表现说明作参考；4、6 等锚点不限制老师填写 5，不把通过分改成范围。 |
| D07 | 后端生成并保存依据，前端每个维度提供按需查看简短依据的交互。 |
| D08 | 依据解释维度为何成立，以及为何建议这个最低分；区分老师明确要求和 AI 推定。 |
| D09 | 题目所有部分和内容，包括维度、分档、通过分和依据均可人工修改；保留现有状态门禁。 |
| D10 | 普通维度修改保留分档与依据，提示老师核对；不后台自动重写或覆盖人工修改。 |
| D11 | 主动“保存并重新生成”仍可替换整套维度及人工修改，但必须在执行前明确提示替换范围并经确认。 |
| D12 | 用户要求现在就用 Trellis 持续记录，并研究真实代码、生成策略和 Deep Agents；仅批准规划及研究，未批准实施或真实模型试跑。 |
| D13 | Deep Agents 必须采用最新发布版本，不使用已经过时的版本迁就旧设计；实施与交付前需实时核查。 |
| D14 | 接受等待很久，但加载区域必须显示当前真实阶段及生成中的可读流式反馈，而不是无动态进展的加载提示。 |
| D15 | 整个生成过程的公开流式输出及其上下文很重要，需要保留和管理，不能只留下最终结果或简短过程摘要。 |
| D16 | 现在需要设计并接入 checkpoint，目标是当前 Docker 运行中的 PostgreSQL；同题执行连续性不能继续以“只做 M0”为由排除。 |
| D17 | Deep Agents backend、消息/状态/上下文机制应为 M1 的中断恢复、询问用户和多轮聊天准备；不等于已批准现在上线 M1 业务入口。 |
| D18 | 用户指定参考 langchain-dev-guide 的踩坑经验及官方 custom middleware 文档，研究 hook 风格与状态管理；仍是规划和研究授权。 |
| D19 | 用户明确确认不做自动相似题参考；M0 只依据本题材料和本题上下文生成维度，不自动检索或继承其他题的要求，不接跨题 Store；本题完整过程、持久化和恢复保留。 |
| D20 | 用户接受“少量关键分数描述，加上 AI 建议通过分对应的描述”，不必为全部 11 个整数单独生成；老师仍可填任意整数，普通改分不自动补档或覆盖。 |
| D21 | 用户确认当前项目数据库全部是测试数据，可随本次重构一次性全部删除，完成后自行重新上传；不要求迁移旧题/旧修改/旧运行。清理限定当前本地项目两库，原始样本保留，执行仍等待最终实施批准。 |
| D22 | 重构过程中必须从 `.local-samples` 会话日志和材料生成有真实来源的测试数据，完成真实数据集成与冒烟测试；用户以后重新上传不能代替开发验收。 |
| D23 | 用户确认日常删除题目时一并删除其完整生成过程与上下文，保留现有删除确认门禁，避免题目消失后原文残留；只清该题，不删除其他题或原始样本。 |

## 明确否决或纠正

- 不再追问 M1 新稿是否通过来定义 M0 本轮范围。
- 不把参考样本的整体相似程度作为本轮产品目标。
- 不改成“选择档位起点作为通过分”，不把锚点分数变成输入白名单。
- 不把分档描述误称为 M1 流程。
- 不等待所有问题问完才记录已确认要求。
- 不把旧记忆中的“优先单题判定还是场景能力诊断”继续当作本轮尚未回答的上游问题；用户已经明确 M0 范围。

## 此前框架偏好与当前边界

- 用户倾向采用 `create_deep_agent`，认为其自主性和 harness 更完整；需要结合实际工作量、工具需求、质量证据及成本进行调研。
- 后续 D13/D14 明确最新 Deep Agents 与流式可见性要求；不再把“是否愿意接受长等待”留作未回答的问题。具体流式 API、预算和传输合同仍由技术设计落实，不等于实施已获批准。
- 用户询问当前维度和分数是否同一次生成，以及新增各部分应一次还是分批生成；这些是研究任务，不是要求用户来回答的业务问题。

## 2026-09-05 技术研究进展，不是新增用户决定

- 已查实当前没有 `create_agent`，而是 `with_structured_output(..., method="function_calling")` 的一次逻辑联合生成，同时输出维度和整数通过分。
- 已检查 Deep Agents 0.7.13 官方 wheel 的 API/依赖，未安装或执行；框架内部构建于 `create_agent`，不是替代 LangChain 的另一种模型。
- 候选方向为受限 Deep Agents 按需读材料、联合输出完整 rubric、校验后有限修订；不默认启用子代理、宿主机文件访问、web、shell、跨题记忆或数据库写工具。
- `research/generation-strategy.md` 保存现状、官方来源、三策略对照和效果验证需求；`design.md`、`implement.md` 是用户要求提前记录的草案，尚未得到最终批准。
- 后续已接受较长等待并明确保留完整公开过程；跨题范围、锚点密度和旧数据处置分别由 D19/D20/D21 收敛。真实样本要求见 D22，不要求用户代查代码或选择框架 API。

## 本轮新增研究证据

- 实时遍历官方 PyPI 的非 yanked 发布，最新正式版和最新任意发布均仍为 0.7.13；这是本次查询结论，不是将来的版本固定承诺。
- 直接读取官方 Markdown 正文后确认推荐 Event Streaming v3；本轮正文未见 Beta 标记，不沿用初步核查中未经支持的 Beta 断言。优先核对实际发布包的 API 合同和 relay 流式路径后使用。
- 当前页面只有可见时轮询与固定加载文案，没有实际阶段/流式进度。现有 Worker 心跳不等于可读的任务推进记录。
- 研究方案是 Worker 事件 → 受控公开进度 → 当前作业进度存储 → 同源 SSE → 加载时间线；D15 又明确完整公开内容留存，D16/D17 增加执行恢复基础。这些尚未实施。

## 本轮持久化核查与提案更新

- 当前 Docker PostgreSQL 17 实例可只读连接：业务库 skill_eval，专用检查点库 skill_eval_checkpoint。检查点库已有迁移 9 和 4,415 行 checkpoint；没有读消息内容、迁移、清理或重放旧线程。
- 当前 M0 app 没有消费相关 checkpoint 环境变量，也未安装 Deep Agents/LangGraph/Postgres checkpointer；不能因配置与表存在而声称已接入。
- 已直接核对最新 wheel：Deep Agents 0.7.13、langgraph-checkpoint-postgres 3.1.2；指定技能里的 StateBackend(runtime)、harness_profile 参数等旧写法不能直接照抄。
- 新增 runtime-design.md，将完整公开消息、图状态、业务状态、调度状态和跨题 Store 分开；此前“不接 checkpoint”和“只保留简短过程”的提案失效。
- 同题记忆已确定；跨题范围后续讨论见下一节。没有安装、建表、修改 .env、调用模型或取得最终实施批准。

## 2026-09-06 本题独立与相似题参考范围已确认

- 用户先表达倾向：老师在一道题确认的要求只作用于这道题，不影响后续其他题；担心题量增大后影响难以控制。
- 用户先将相似题参考作为脑暴选项，随后对“本次不自动检索相似题、只用本题材料与上下文”明确回答“明确不做”；据此新增 D19，不再保留为待定问题。
- 分析：检索少量候选可以减少上下文数量，但不能证明旧题要求适用。需要保护的是标准来源和适用边界，不只是 token 或题库规模。
- 当前确认范围：本次不自动检索或注入其他题，保留本题材料、完整公开过程、StateBackend 和 PostgreSQL checkpoint；不接跨题 Store，不后台学习通用老师偏好。
- 现有 memory_materials 只是本题提供的材料，不是平台长期记忆库。老师把旧内容明确整理进本题材料时，按本题快照处理；不要求新增搜索、引用或确认 UI。
- 若未来有明确复用需求，可另行讨论候选检索和明确纳入材料；不承诺仅靠相似度或“只供参考”标签就能阻止历史标准影响当前生成。
- 本题隔离已纳入 PRD 验收与 runtime-design 第 7 节：其他题的变化不能进入本题生成输入或触发本题更新；恢复、摘要与重试同样受约束。
- 后续锚点密度已由用户“我接受”的答复确认，记录为 D20。建议分有独立表现描述是初始 AI 生成要求，不是老师改分必须从锚点里选的限制；编辑保存与生成结果的校验必须分开。
- 旧题保留范围后续由 D21 明确为当前项目测试数据可清理；此前“先全部保留”的待定建议不再作为当前切换要求。该授权不扩展到未来新数据或原始样本。
- 本轮仅更新规划与检查文档；不安装、不调用模型、不修改产品/数据库、不启动实施。

## 2026-09-06 真实样本测试与旧库重置

- 本次只读盘点 `.local-samples/m0`：财报供稿和 MEGA 新闻稿两组，共 6 个业务文件；worktree 未自带被忽略的样本目录，需要显式只读源路径。
- 已定位财报老师终稿、对应批评及 MEGA 老师提供的正式主稿。导出中存在系统/技能指令、思考注记、tool_result、被替代要求和后续不同任务，不能把所有内容都当本题老师标准。
- 当前真实验收入口仍有 SQLite、内置样例、前 800 字截断、缺失答案占位和旧两字段断言等限制；已把更新入口及移除这些验收捷径纳入实施清单。
- 新增 research/real-sample-validation.md 记录文件 hash、定位依据、测试数据生成、样本缺口和真实集成/冒烟/main 复验门禁；没有生成产品 fixture 或执行测试。
- 日常题目删除后续已由 D23 确认一并清理新增完整过程和 checkpoint；不再作为待定项。当前旧测试数据重置与日常显式删除是两个独立授权边界。

## 2026-09-06 关联删除设计与最终审阅

- 已查现行删除门禁为鉴权/revision、生成中禁止、已发布先重开、曾发布按完整标题 NFC/strip 确认；仓储原子删除题目和生成 jobs/attempts。批次回执不含材料正文，仅保存 hash、case/question ID 与状态。
- 当前 DELETE 立即返回 204，前端随即跳转；接入另一个库的 checkpoint 后不能沿用这一早退成功语义。提出复用 OperationJob 的持久清理：先按原门禁受理并冻结同题，清理所有 thread/checkpoint，再原子清业务原文与提交删除作业成功；失败可见并可重试。
- 当前 Worker 初始化和错误投影偏向 rubric_generation；新增删除任务须隔离任务职责，避免生成 Provider 不可用阻塞清理，或清理任务把自己删掉、被误投影为生成失败。
- 已将跨库中断、迟到写回、全部历史 thread、无原文回执、源文件保护和真实删除验收纳入 runtime-design/implement；这是设计，不是已执行清理。
- PRD 已完成收敛整理，D01-D23 的业务问题已回答；review.md 汇总最终范围、步骤、验收与实施技术验证。工程预算和 SDK/网关能力在获批后验证，不继续要求用户选择 API。
- 最终状态仍为 planning，等待用户在本摘要之后明确批准实施；没有产品修改、安装、模型调用、测试、清库或提交/合并。

## 2026-09-06 父子任务拆分

- 用户要求继续按 Trellis 拆分父/子任务；这是任务组织与规划授权，不修改 D01-D23，也不是实施批准。
- 保留原任务为父任务，使用原生 task.py create --parent --no-start 建立 C1 样本、C2 运行基础、C3 业务一次性切换、C4 系统验收、C5 本地切换五个子任务。
- 每个子任务写入独立 PRD/design/implement、非空 spec/research context 与显式依赖；parent/children 只表示归属，meta.depends_on 不会被 Trellis 自动执行，需在启动前核对 main 交付证据。
- C3 保持合同、生产接入、页面、关联删除与旧路径清理的原子交付；不为拆分而保留临时兼容或让 C4 补齐基础功能。当前旧项目两库的 reset 只归 C5。
- 父任务 task-map.md 与共享文档同步分派、规划登记基线、子 worktree/main 门禁及私有证据保留。子任务实际 branch/worktree_path 仍为空，未来位置只写在 meta。
- 本轮没有 task.py start、实施分支/worktree 创建、产品修改、依赖安装、模型调用或清库；所有父子任务保持 planning。

## 已核对的规划环境

- 主工作区：`/Users/hsikey/Company/skill-eval-platform`，保持 `main`，本轮建档前干净。
- 本轮基线：`009117cd025cb1b5f4c25f4d6c4f011fb84d1797`。
- 实时远端 main：`f8fbb0719028c9e7435d990e100f2919927e89d5`，本地 main 领先 18 个提交，无落后；本轮不推送或合并。
- 专属规划 worktree：`/Users/hsikey/Company/skill-eval-platform-wt/m0-rubric-anchors-evidence`。
- 分支：`codex/m0-rubric-anchors-evidence`，base branch 为 `main`。
- 旧记忆中提及的 `rubric-dimension-rethink` 任务、分支和 worktree 在本轮现场均不存在，因此新建独立规划记录，不把旧访谈假设当作现状。
- 仅从主工作区复制了被忽略的 `.env`，没有读取或输出凭证；没有实际模型调用，没有产品代码改动，没有 `task.py start`。
- 本轮尚未重跑产品测试或构建；上述 Git 状态不能替代质量验收。
