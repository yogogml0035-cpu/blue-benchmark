# C2 实施与验收

## 前置门禁

- 确认 C1（09-06-m0-rubric-real-samples）完成记录可在 active/archive 找到，且交付提交已被 main 包含并通过 main 复验；task.py 不会自动执行 depends_on 门禁。
- 取得明确实施批准后从最新已验证 main 创建 codex/m0-rubric-deep-runtime 独立 worktree；不沿用父任务或 C1 工作区。
- 复制 .env 并显式设置本任务独占验收目标；只通过 C1 已提交代码重建真实材料。

## 执行顺序

1. 实时读取官方版本/API，确认依赖与当前模型工厂，再更新现有 backend/pyproject.toml 和 uv.lock。
2. 实现受限运行构造、材料接口与公开事件归一，验证默认子代理、工具和文件权限封闭。
3. 接入低层 PostgresSaver/StateBackend、连接/锁/加密和不依赖模型的线程清理；不添加业务路由或题目 schema。
4. 增加 middleware/state update、同线程重启、重复输入、并发写、失租/断线、中断恢复及完整删除的定向测试。
5. 在有限预算下以 C1 真实输入跑显式底层探针，保存脱敏版本/运行证据；不宣称生产 Worker 或浏览器闭环通过。
6. 更新本组件实际接口说明与适用规格，完成分支/main 质量门后交接 C3。

## 质量门

```bash
git diff --check
make test
make build
```

- 另跑新运行原语测试、隔离 PostgreSQL 测试与真实 Provider 探针，核查原产品入口未改变。
- 验证指定最新版本的实际签名，不以旧技能、InMemorySaver、环境变量存在或空表冒充持久化证据。
- 有失败即停止交接，禁止静默降级模型/SDK、把数据库错误改为内存模式或保留第二个生产路径。

## 完成与回退

- 记录 SDK/模型身份、已验证能力与未涉及的生产层边界、提交 SHA 和 main 复验结果；与 C1 相同，原始语料不能被修改。
- 回退只撤销本任务运行组件和依赖变更，清理自有验收资源；不触碰当前项目旧数据或原始样本。
- 当前任务完成不意味着父任务完成；C3 必须接入业务状态与 API 后再次进行真实闭环验证。
